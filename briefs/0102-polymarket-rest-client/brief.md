# Polymarket REST Client

HTTP client for both the CLOB API (order/market data) and Gamma Markets API (condition metadata). API key auth. Rate limit awareness. Retry with exponential backoff.

## Done when

- Can authenticate and fetch a single market's condition metadata
