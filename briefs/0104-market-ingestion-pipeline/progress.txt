2026-03-09 Completed checkpoint 1: added core resolution hash, logger, ingestion-run repository, config exports, and passing core tests.
2026-03-09 Completed checkpoint 2: added Kalshi cursor pagination and expanded client coverage for cursor flow and param forwarding.
2026-03-09 Completed checkpoint 3: added Kalshi and Polymarket normalizers, fetch-all helpers, ingestion package dependency updates, and passing normalizer tests.
2026-03-09 Completed checkpoint 4: added the ingestion orchestrator with per-market error isolation, resolution hashing, run tracking, and passing orchestrator tests for both platforms.
2026-03-09 Completed checkpoint 5: added the scheduler, backfill CLI, entrypoint bootstrap, DB directory creation, and full-plan verification (`bun test` plus backfill help output).
2026-03-09 Added `--sample`/`--limit` backfill support, verified with targeted tests and a real sample run into `data/manual-sample.db` (3 Kalshi + 3 Polymarket markets, both runs completed).
2026-03-09 Updated ingestion defaults to request active/open markets (`open` for Kalshi filter, `active=true closed=false` for Polymarket), added coverage, and reran a fresh sample into `data/manual-sample-filtered-fresh.db`.
2026-03-09 Added Kalshi parlay filtering using `mve_selected_legs`; tests pass, but a live sample over the first 500 open Kalshi markets found zero non-parlay matches, so a no-parlay sample backfill currently stalls while scanning the feed.
2026-03-09 Switched Kalshi exclusion to API-side `mve_filter=exclude`, removed redundant post-fetch filtering, and verified with a fresh sample in `data/manual-sample-api-filter.db` showing non-parlay Kalshi markets.
2026-03-09 Ran `bun run packages/ingestion/src/backfill.ts --sample --limit 5` against `data/plan-verification-sample.db`; first pass inserted 5 Kalshi + 5 Polymarket active markets with completed ingestion runs and 64-char hashes, second pass updated the same 10 rows (0 inserts) confirming idempotent active-market ingestion aligned with the plan/overview intent.
2026-03-09 Ran `bun run packages/ingestion/src/backfill.ts --sample --limit 500` against `data/plan-verification-500.db`; both platforms completed with 500 fetched/inserted, 0 updated, 0 errored, 500 active rows per platform, and no missing or malformed resolution hashes.
2026-03-09 Reran `bun run packages/ingestion/src/backfill.ts --sample --limit 500` against `data/plan-verification-500.db`; both platforms completed with 500 fetched, 0 inserted, 500 updated, 0 errored, `ingestion_runs` recorded completed update-only passes, and `markets` remained stable at 1000 total rows.
2026-03-09 Spot-checked rerun rows in `data/plan-verification-500.db`; sampled Kalshi and Polymarket rows kept stable platform IDs, had `updated_at > created_at` after the rerun, preserved expected normalized nullability/metadata per platform, and retained 64-char resolution hashes plus raw payload storage.
2026-03-09 Added additive migration `packages/core/drizzle/0001_matching_shape_fields.sql` for the new matching-oriented market columns, reran targeted DB/ingestion tests, and successfully backfilled the default DB with `bun run packages/ingestion/src/backfill.ts --limit 100` (100 inserted for Kalshi, 100 inserted for Polymarket, 0 errored).
2026-03-09 Fixed backfill `--limit` semantics in `packages/ingestion/src/orchestrator.ts` so large limits cap total fetched markets without exceeding platform page-size ceilings (Kalshi 1000, Polymarket 500), hardened `GammaClient` array parsing against malformed market payloads, added regression coverage, and verified `bun run packages/ingestion/src/backfill.ts --limit 10000` completed successfully (Kalshi 10000 fetched, Polymarket 10000 fetched, 0 errored on both).
2026-03-09 Added `start` and `backfill` scripts to `packages/ingestion/package.json` so the scheduler and one-shot backfill have discoverable package-level entrypoints.
2026-03-09 Added startup logging to `packages/ingestion/src/index.ts` and `packages/ingestion/src/backfill.ts` so scheduler/backfill runs log configuration, migration startup, and client initialization as soon as the process begins; verified with targeted ingestion tests.
2026-03-09 Fixed the scheduler timeout path in `packages/ingestion/src/scheduler.ts` so overdue cycles queue exactly one follow-up run instead of overlapping active cycles, shutdown now waits for the full queued chain before closing SQLite, and new regression tests plus `bun test` pass.
2026-03-09 Hardened scheduler stall recovery by threading `AbortSignal` through ingestion fetch paths, aborting timed-out platform requests before queueing the next cycle, and verifying with targeted scheduler/orchestrator/shared-platform tests.
2026-03-09 Followed up on review findings: preserved the active cycle stall timeout during scheduler shutdown so hung fetches still abort before SQLite closes, updated Polymarket normalization to map `active=false && closed=false` markets to `suspended`, and verified with targeted scheduler and normalizer tests.
